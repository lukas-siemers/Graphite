// Re-export the spatial ink types from @graphite/canvas so editor consumers
// don't have to import from two packages just to type a stroke.
export type { SpatialInkStroke, StrokePoint } from '@graphite/canvas';
