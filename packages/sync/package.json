{
  "name": "@graphite/sync",
  "version": "0.1.0",
  "description": "Graphite sync engine — Phase 2 (requires Supabase subscription)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
