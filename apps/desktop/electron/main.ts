/**
 * Graphite — Electron main process
 * Phase 3 implementation.
 *
 * Rules (from CLAUDE.md):
 * - All Node.js APIs (fs, better-sqlite3, shell) live exclusively in this file and files it imports.
 * - The renderer is a pure web context — never import Node APIs there.
 * - Expose functionality to the renderer ONLY via contextBridge IPC.
 */

// Phase 3 placeholder — BrowserWindow + contextBridge setup goes here.
// See CLAUDE.md Phase 3 deliverables for the full implementation spec.

export {};
